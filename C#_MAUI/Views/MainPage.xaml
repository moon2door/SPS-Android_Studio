<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="_SPS.Views.MainPage"
             xmlns:viewmodels="clr-namespace:_SPS.ViewModels"
             xmlns:models="clr-namespace:_SPS.Models"
             Title="홈">

    <ContentPage.BindingContext>
        <viewmodels:MainViewModel />
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="추가" Command="{Binding NavigateToAddPetCommand}" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto, Auto, *">

        <VerticalStackLayout Grid.Row="0" Padding="20" Spacing="5" BackgroundColor="#f0f0f0">
            <Label Text="보호 중인 동물들" FontSize="22" FontAttributes="Bold" TextColor="Black"/>
            <Label Text="{Binding UserEmail, StringFormat='관리자: {0}'}" FontSize="14" TextColor="Gray"/>
        </VerticalStackLayout>

        <SearchBar Grid.Row="1" 
                   Placeholder="이름 또는 종 검색..." 
                   Text="{Binding SearchText}"
                   SearchCommand="{Binding SearchCommand}"
                   Margin="10,5"
                   BackgroundColor="White"
                   CancelButtonColor="Gray"/>

        <ActivityIndicator Grid.Row="2" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                           Color="Blue" VerticalOptions="Center" HorizontalOptions="Center" />

        <CollectionView Grid.Row="2" 
                        ItemsSource="{Binding Pets}" 
                        SelectionMode="None"
                        Margin="10">

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:PetModel">
                    <Frame Margin="0,5" Padding="10" CornerRadius="10" HasShadow="True">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer 
                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MainViewModel}}, Path=GoToDetailCommand}"
                                CommandParameter="{Binding .}" />
                        </Frame.GestureRecognizers>
                        <Grid ColumnDefinitions="60, *, Auto" ColumnSpacing="10">
                            <Frame Grid.Column="0" Padding="0" CornerRadius="30" HeightRequest="60" WidthRequest="60" IsClippedToBounds="True" BorderColor="LightGray">
                                <Image Source="{Binding ImageUrl}" Aspect="AspectFill" HeightRequest="60" WidthRequest="60"/>
                            </Frame>
                            <VerticalStackLayout Grid.Column="1" Spacing="5" VerticalOptions="Center">
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" TextColor="Black"/>
                                <HorizontalStackLayout Spacing="10">
                                    <Label Text="{Binding Species}" TextColor="Blue" />
                                    <Label Text="{Binding Age, StringFormat='| {0}'}" TextColor="Gray" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                            <Label Grid.Column="2" Text="＞" VerticalOptions="Center" TextColor="LightGray"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>